apiVersion: v1
kind: Service
metadata:
  name: {{ include "aqua-os.fullname" . }}-calendar
  labels:
    {{- include "aqua-os.labels" . | nindent 4 }}
    app.kubernetes.io/component: calendar
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.calendar.service.port }}
      targetPort: {{ .Values.calendar.service.port }}
      protocol: TCP
  selector:
    {{- include "aqua-os.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: calendar
